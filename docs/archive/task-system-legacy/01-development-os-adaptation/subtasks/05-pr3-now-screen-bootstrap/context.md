# Subtask Context: PR3 Now Screen Bootstrap

## Goal

Ship the first visible node-centered product UI without cutting over the legacy card workflow.

## Inputs

- Parent epic `plan.md` and `architecture.md`.
- PR1 storage foundation already implemented in runtime code.
- Existing legacy Library/Study UI in `src/App.jsx`.

## Why This Lane Exists

- The user validated that PR1 infrastructure is stable and explicitly approved moving to the first visible product slice.
- The next smallest meaningful step is a thin `Now` surface, not a full map/tree/node suite.
- Legacy card UX must stay reachable while the new recommendation loop becomes real.

## Delivered Outcome

- A visible `Now` tab in the running app.
- A starter workspace path for empty PR1 hierarchy storage.
- A primary recommendation card plus first daily-session entry action.