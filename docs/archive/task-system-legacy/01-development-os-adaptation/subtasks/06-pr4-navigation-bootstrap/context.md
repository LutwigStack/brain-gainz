# Subtask Context: PR4 Navigation Bootstrap

## Goal

Introduce the first browseable hierarchy surface so the user can navigate beyond the recommendation queue.

## Inputs

- Parent epic `plan.md` and `architecture.md`.
- Completed `05-pr3-now-screen-bootstrap` runtime slice.
- Existing hierarchy persistence and recommendation logic from PR1/PR3.

## Why This Lane Exists

- `Now` is now strong enough to execute work, but it is still not enough to browse the whole hierarchy.
- MVP explicitly requires `Sphere Map`, `Skill Tree`, and `Node Screen` navigation surfaces.
- The next thin slice is a browseable map surface, not the full PR4 breadth.

## Delivered Outcome

- A visible `Map` tab.
- Sphere-level overview cards.
- Direction/skill tree with selectable nodes.
- Read-only node screen over the selected hierarchy item.