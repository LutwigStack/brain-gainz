# Task System Legacy Archive

This directory stores the previous task-system structure that used:

- duplicated registries
- `context.md` and `architecture.md` at multiple levels
- folder-based `subtasks/`
- mandatory `review/` directories

The active task system now lives only under `tasks/`.
